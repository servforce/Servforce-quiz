参考qml.md文件，开发一个基于markdown的在线能力测试系统。管理员能够快速选择markdown试卷生成链接/二维码发给候选人，候选人完成身份验证后在线作答，系统通过对客观题自动判分和大模型对简答题进行判分，得到最终分数，来评估是否进入面试。 
1. 管理员先输入账号密码进行登录，跳转到管理员界面，管理员通过点击上传markdown文件按钮，选择文件夹中的markdown试卷，上传到考试系统中，立刻解析markdown试卷文件，将试卷中的题号，类型，分数，题目，选项，正确答案，评分标准，分别存入到Json文件中。通过调取题目，选项生成候选者版试卷，管理员加入候选者个人信息，生成对应候选者的唯一答题链接或二维码，发送给候选者。
2. 候选者点击答题链接，进行候选者身份验证，输入姓名，手机号等相关信息，与数据库中的候选者相关信息进行比较，信息完全正确，则跳转到答题界面；反之，则重新输入个人信息，当达到输入信息的最大次数时，链接失效。进行答题界面，候选者数据表中的答题状态变成已验证通过，同时开始计时，达到限制时间，则自动收卷，或用户点击提交按钮提前交卷，答题状态修改为结束答题。
3. 接收答题试卷后，开始自动判卷，客观题通过从Json文件中调取正确答案，与候选者答题结果进行对比，正确得满分，错误不得分，主观题通过从Json文件中调取评分标准，作为大模型提示词和候选者答案，同时发送给大模型，进行评分，并给出得分理由，通过求和主客观题分数，是否达到阈值，达到则进行面试。
接下来，我需要你给我提供一个详细的设计文档，包含数据库的定义(postgresql)、主要功能模块和任务的定义，http接口部分定义，前端页面的设计，使用python实现，http使用Flask，将设计文档保存在项目根目录下，文件名为syetem-design.md

只参考qml.md文件，开发一个基于markdown的在线能力测试系统。管理员能够快速选择markdown试卷生成链接/二维码发给候选人，候选人完成身份验证后在线作答，系统通过对客观题自动判分和大模型对简答题进行判分，得到最终分数，来评估是否进入面试。  
1. 管理员先输入账号密码进行登录，跳转到管理员界面，管理员通过点击上传markdown文件按钮，选择文件夹中的markdown试卷，上传到考试系统中，立刻解析markdown试卷文件，将试卷中的题号，类型，分数，题目，选项，正确答案，评分标准，分别存入到Json文件中。通过调取题目，选项生成候选者版试卷，管理员加入候选者个人信息，生成对应候选者的唯一答题链接或二维码，发送给候选者，答题状态变为试卷发送。
2. 候选者点击答题链接，进行候选者身份验证，输入姓名，手机号等相关信息，与数据库中的候选者相关信息进行比较，信息完全正确，则跳转到答题界面；反之，则重新输入个人信息，当达到输入信息的最大次数时，链接失效。进行答题界面，候选者数据表中的答题状态变成已验证通过，同时开始计时，达到限制时间，则自动收卷，或用户点击提交按钮提前交卷，答题状态修改为结束答题。
3. 接收答题试卷后，开始自动判卷，客观题通过从Json文件中调取正确答案，与候选者答题结果进行对比，正确得满分，错误不得分，主观题通过从Json文件中调取评分标准，作为大模型提示词和候选者答案，同时发送给大模型，进行评分，并给出得分理由，通过求和主客观题分数，是否达到阈值，达到则进行面试，将成绩，答题时间，是否进入面试，给出成绩理由存入到候选人表中。
接下来，边界条件是：除了候选人列表(包括主键ID，姓名，电话，答题状态(send：试卷发放，verified：已验证，finished：试卷完成)、成绩(默认0，成绩区间0-100)、答题时间、是否进入面试(默认否)、备注(评价)，剩下尽量的不使用数据库，markdown解析的内容和候选者答题记录都放到json文件中。我需要你给我提供一个详细的设计文档，包含数据库的定义(postgresql)、主要功能模块和任务的定义，http接口部分定义，前端页面的设计，使用python实现，http使用Flask，将设计文档保存在项目根目录下，文件名为syetem-design.md