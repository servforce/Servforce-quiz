# PostgreSQL 默认账号与库名。
# docker compose 的 `db` 容器会直接读取这 3 项；宿主机直跑时 `DATABASE_URL` 也应与它们保持一致。
POSTGRES_USER=admin
POSTGRES_PASSWORD=pasword
POSTGRES_DB=md_quiz

# PostgreSQL 连接串。
# 这个值主要给宿主机直跑 / devctl 使用；docker compose 内的 app 容器会覆盖为 `db:5432`。
DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@127.0.0.1:5433/${POSTGRES_DB}

# 应用会话签名密钥。
# 用于 SessionMiddleware 的 cookie 签名，生产环境必须改成随机长字符串。
APP_SECRET_KEY=change-me

# 后台管理员登录账号。
ADMIN_USERNAME=admin

# 后台管理员登录密码。
ADMIN_PASSWORD=password

# Web 监听地址和端口。
# 当前开发入口默认与 README、docker-compose 保持一致，使用 8000。
APP_HOST=0.0.0.0
PORT=8000

# assignment token 的独立签名密钥。
# 建议与 APP_SECRET_KEY 分开设置；不设置时会回退使用 APP_SECRET_KEY。
ASSIGNMENT_TOKEN_SECRET=change-me

# 可选：只用于 Git 仓库同步的代理地址；会通过 `git -c http.proxy=...` 显式传入，不影响应用进程其它网络请求。
EXAM_REPO_SYNC_PROXY=

# MCP（远程 HTTP）
# 是否启用 MCP 服务端。启用后会挂载到 /mcp。
MCP_ENABLED=0
# MCP Bearer Token。启用 MCP 时必须设置。
MCP_AUTH_TOKEN=change-me
# 允许浏览器型 MCP 客户端访问时使用，逗号分隔。举例：http://localhost:3000,http://127.0.0.1:3000
MCP_CORS_ALLOW_ORIGINS=

# LLM (OpenAI-compatible Responses API)
# 当前实现明确依赖 OpenAI-compatible 的 Responses API。
# 只兼容 `chat/completions` 的平台，不能只改 `OPENAI_BASE_URL` 直接接入。
# 大模型 API Key。
OPENAI_API_KEY=<your_api_key>
# 大模型名称；当前默认值示例对应火山引擎方舟 / 豆包模型。
OPENAI_MODEL=doubao-seed-2-0-pro-260215
# Responses API 基础地址，而不是仅 `chat/completions` 的兼容地址。
OPENAI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# 是否要求 Responses 接口返回 JSON 格式。
LLM_RESPONSE_FORMAT_JSON=0

# LLM retries/timeouts
# 大模型请求最大重试次数。
LLM_RETRY_MAX=3
# 短答判卷（JSON 输出）超时时间（秒）。
LLM_TIMEOUT_JSON=300
# 自由文本调用超时时间（秒）。
LLM_TIMEOUT_TEXT=90
# 通用结构化调用超时时间（秒）。
LLM_TIMEOUT_STRUCTURED=300
# 图像理解调用超时时间（秒）。
LLM_TIMEOUT_VISION=120

# 简历解析相关的 prompt 截断长度控制。
RESUME_DETAILS_TEXT_MAX_CHARS=80000
RESUME_DETAILS_FOCUS_MAX_CHARS=60000
RESUME_EXPERIENCE_RAW_MAX_CHARS=60000
RESUME_DETAILS_PROMPT_MAX_CHARS=90000

# SMS Verification (Aliyun PNVS / DYPNS 2017-05-25)
# 最小必填：跑通短信认证只需要下面 4 项。
# 阿里云 AccessKey ID。
ALIYUN_ACCESS_KEY_ID=<your_aliyun_access_key_id>
# 阿里云 AccessKey Secret。
ALIYUN_ACCESS_KEY_SECRET=<your_aliyun_access_key_secret>
# 短信认证签名名称。
ALIYUN_PNVS_SIGN_NAME=速通互联验证服务
# 短信认证模板编号。
ALIYUN_PNVS_TEMPLATE_CODE=100001

# 可选高级配置：只有在默认行为不满足时再填写。
# 号码认证服务 endpoint；默认 dypnsapi.aliyuncs.com。
ALIYUN_PNVS_ENDPOINT=dypnsapi.aliyuncs.com
# 号码认证服务地域；一般可留空。
ALIYUN_PNVS_REGION_ID=
# 支持占位符：##code##（验证码），##min##（有效期分钟数）
ALIYUN_PNVS_TEMPLATE_PARAM={"code":"##code##","min":"##min##"}
# 可选：短信认证方案名。
ALIYUN_PNVS_SCHEME_NAME=
# 可选：国家码。
ALIYUN_PNVS_COUNTRY_CODE=86
# 可选：业务透传 ID。
ALIYUN_PNVS_OUT_ID=
# 可选：验证码有效期（分钟）。
ALIYUN_PNVS_VALID_TIME=5
# 可选：校验策略。
ALIYUN_PNVS_CASE_AUTH_POLICY=
